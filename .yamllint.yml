extends: default
rules:
  line-length:
    max: 160
  truthy:
    allowed-values: ["true", "false", "yes", "no"]
    check-keys: false
  document-start: disable
  comments:
    min-spaces-from-content: 1
ignore: |
  .github/
