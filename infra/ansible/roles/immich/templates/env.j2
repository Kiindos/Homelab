# {{ ansible_managed }}
UPLOAD_LOCATION={{ immich_bibliotheque }}
DB_DATA_LOCATION={{ immich_dossier }}/postgres
IMMICH_VERSION={{ immich_version }}
TZ=Europe/Paris
DB_PASSWORD={{ immich.db_password }}
DB_USERNAME=immich
DB_DATABASE_NAME=immich
IMMICH_CONFIG_FILE=/config/immich.json
# Le WAF est le seul mandataire de confiance.
IMMICH_TRUSTED_PROXIES={{ hostvars['waf01'].adresse_ipv4 }}
